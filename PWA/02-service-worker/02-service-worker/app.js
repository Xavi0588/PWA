// Adahir Osorio Lopez
if(navigator.serviceWorker) { //En esta linea de código nos dice (Si se navega en el service worker)
    console.log("se puede utilizar"); //que la consola me muestre el texto "Se puede utilizar"
}
